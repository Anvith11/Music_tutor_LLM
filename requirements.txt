# Qwen2-Audio Music Tutor Requirements

# Core ML/AI Dependencies
torch>=2.0.0
transformers>=4.45.0
tokenizers>=0.19.1
accelerate>=0.21.0

# Flash Attention (GPU optimization - optional, CUDA only)
# Uncomment the line below if you have a compatible NVIDIA GPU with CUDA:
# flash-attn>=2.5.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.1
numpy>=1.24.0

# For quantization and memory optimization (optional)
# Uncomment if you need quantization support:
# autoawq>=0.2.9
# bitsandbytes>=0.41.0

# Utility packages
tqdm>=4.65.0
requests>=2.31.0
pillow>=10.0.0  # For any image processing in multimodal scenarios

# Development and testing
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0 